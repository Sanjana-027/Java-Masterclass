public class DecimalComparator{
    
    public static boolean areEqualByThreeDecimalPlaces(double num1, double num2) {
        
        int roundNum1 = (int)(num1 * 1000);
        int roundNum2 = (int)(num2 * 1000);
        
        if (roundNum1 == roundNum2)
            return true;
            
        return false;
    }
}
